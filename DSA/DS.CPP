#include <stdio.h>
#include <vector>
using namespace std;

// https://github.com/gibsjose/cpp-cheat-sheet/blob/master/Data%20Structures%20and%20Algorithms.md

// STRING
string s = "";
s.begin;
s.end;
swap;
find;
substr;
compare;
append;
push_back;
replace;
size();
length;


// extracts characters from is until delim or until it finds \n
getline(is, delim);


// VECTOR //
vector<int> vc(10, 0);
vector<int> vect = {0, 1, 2, 3};
v.insert(v.begin(), value);             // head
v.insert(v.begin() + index, value);     // index
v.push_back(value);     
v.pop_back(); // quita el ultimo

// MAP //
// O(log(n))
// unordered_map O(1)
map<int, int> mp;
mp[key] = value;


// TEMPLATE //

// BINARY SEARCH // 

// SET // 

// STACK //
push
pop
top
isFull
isEmpty


